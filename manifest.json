{
   "manifest_version": 2,
   "content_scripts": [ {
      "css": ["fb-bottom-spacer.css"],
      "matches" : [
     "http://*.facebook.com/*",
     "https://*.facebook.com/*"
   ]
   } ],
   "description": "Adds space below a post so that chatbox doesn't overlap it",
   "name": "fb-bottom-spacer",
   "version": "0.1"
}

